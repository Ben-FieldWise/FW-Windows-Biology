const test=require("node:test");const assert=require("node:assert/strict");const{stages,templates,newInvestigation,addMeasurement,qualityFlags,investigationProgress,submissionReadiness,weatherSuitability,measurementsCsv,investigationReport,sanitizeTransferValue}=require("../electron/biology-contracts.cjs");
test("Biology inquiry follows the eight-stage iOS contract",()=>{assert.deepEqual(stages,["ask","predict","observe","record","compare","explain","evaluate","conclude"]);assert.equal(templates.length,5);assert.deepEqual(templates.map(({id})=>id),["ecology","microscopy","genetics","physiology","plant"]);});
test("investigations preserve Core launch identity and offline sites",()=>{const value=newInvestigation("river",{taskId:"task",classId:"class",studentId:"student",schoolId:"school",title:"River task"},"inv","2026-08-20T00:00:00Z");assert.equal(value.activityId,"task");assert.equal(value.schoolId,"school");assert.equal(value.sites.length,1);assert.equal(investigationProgress(value).percentage,13);});
test("measurements preserve provenance and surface quality flags",()=>{const measured=addMeasurement({}, {fieldId:"temp",rawValue:"21.5",unit:"°C",origin:"measured"});assert.equal(measured.numericValue,21.5);assert.deepEqual(qualityFlags(measured),[]);assert.deepEqual(qualityFlags(addMeasurement({}, {fieldId:"x",rawValue:"5",origin:"estimated"})),["missingUnit","estimatedValue"]);});
test("submission readiness blocks incomplete or unlinked work",()=>{assert.equal(submissionReadiness(newInvestigation()).ready,false);const value=newInvestigation("urban",{taskId:"task",studentId:"student",schoolId:"school"});Object.assign(value,{prediction:"p",explanation:"e",evaluation:"e",conclusion:"c"});value.sites[0].notes="observed";value.sites[0].measurements=[addMeasurement({}, {fieldId:"eqs",rawValue:"4",unit:"/5"})];assert.equal(submissionReadiness(value).ready,true);});
test("weather suitability applies field safety thresholds",()=>{assert.equal(weatherSuitability({wind_speed_10m:12,weather_code:1}).level,"good");assert.equal(weatherSuitability({precipitation:4}).level,"caution");assert.equal(weatherSuitability({wind_speed_10m:55}).level,"unsafe");});
test("Biology exports preserve measurements safely and report the inquiry",()=>{const value=newInvestigation("river",{},"i","now");value.sites[0].measurements.push(addMeasurement(value.sites[0],{fieldId:"width",label:"Channel width",rawValue:"=1+1",unit:"m"}));value.explanation="Velocity reflects channel form.";assert.match(measurementsCsv(value),/^\uFEFF"Site"/);assert.match(measurementsCsv(value),/"'=1\+1"/);assert.match(investigationReport(value),/Explanation\nVelocity reflects channel form\./);});
test("transfer sanitization removes device-local attachment paths",()=>{const value=sanitizeTransferValue({sites:[{attachments:[{id:"a",path:"C:\\Users\\Student\\photo.jpg",name:"photo.jpg"}]}]});assert.equal(value.sites[0].attachments[0].path,undefined);assert.equal(value.sites[0].attachments[0].name,"photo.jpg");});
